# PracTrack Build Progress

Tracks status against the `MASTER_PROMPT.md` build order. Update this file (status + notes) whenever a module's state changes — this is what the next session/agent reads to know where to pick up.

Status values: **Not started** / **In progress** / **Blocked** / **Done**

| # | Module | Status | Notes |
|---|---|---|---|
| 1 | Scaffold (Next.js + TS + Tailwind + shadcn/ui + Prisma + PWA shell) | In progress | Next.js 16.3.4 / React 19.2.8 / Tailwind v4 scaffolded, shadcn/ui initialized (style: base-nova, navy+amber theme pending). Prisma installed & pinned to 6.19.3 (matched CLI+client) after `latest` resolved to an unstable 8.0.0-rc with broken transitive deps. PWA manifest + service worker not yet added. |
| 2 | Full Prisma schema (all PRD §8 entities + AuditLog/Notification/config singletons) | Done | `prisma/schema.prisma` written covering every entity in PRD §8 plus `SchoolSupervisorHistory` (reassignment logging per PRD §6.2). Validated with `prisma validate`. **Not yet migrated — blocked on DATABASE_URL (see Blocked section).** |
| 3 | Auth and role scoping (`requireSession()`, `scopeToRole()`) | In progress | `src/lib/auth.ts` (NextAuth v5 beta, Credentials provider, JWT session carrying role + scope ids), `src/lib/session.ts` (`requireSession`/`requireRole`), `src/lib/scope.ts` (`scopeToRole` fragments per entity: intern/attendance/session/sessionDocument/endOfTermSubmission/alert/school). NextAuth route handler wired. Login UI not yet built. Not yet tested against a live DB. |
| 4 | Audit-log writer | Done | `src/lib/audit.ts` — `writeAuditLog()`, callable inside a transaction. Not yet wired into any mutation (none exist yet). |
| 5 | Config + seed data (CheckInConfig, FlaggingRuleConfig, EvaluationCriterion/Item, effective-check-in resolver) | Done (code) — unrun | `src/lib/check-in-config.ts` (cascade resolver + config-singleton getters), `src/lib/evaluation-score.ts` (weighted scoring, computed server-side per CLAUDE.md). Full seed script under `prisma/seed/` (config, evaluation instrument, 15 real partner schools from the mockups, one academic year/semester/shifting pair, demo accounts matching mockup names). Evaluation instrument seed has REAL item text for Lesson Planning (7) and Teacher's Personality (6) transcribed from the deck; the other 4 criteria (Content, Teaching Methods, Classroom Management, Questioning Skills) are seeded with an explicit single PLACEHOLDER item each and a console.warn, since PRD §10 Q2 is still open — do not treat that wording as final. Dry-ran the seed script against a placeholder DB URL to confirm it fails only at the DB connection step (all logic up to that point, including the weights-sum-to-100 check, runs clean). **Cannot actually execute until DATABASE_URL is live.** |
| 6 | Academic period module (AcademicYear/Semester/Shifting CRUD + activation transition) | Done (untested against live DB) | `validation/shifting.ts`, `services/academic-period-service.ts` (createSemester auto-creates both fixed shiftings; configureShifting edits dates/requirements; activateShifting is the single-active-shifting transaction — previous ACTIVE → COMPLETED, target → ACTIVE, all inside one transaction, audit-logged). Note in the code: "resetting counters" on activation is not a write at all — every downstream query is already shiftingId-scoped, so a freshly-activated shifting has zero rows and reads as zero automatically; nothing needs to be zeroed out in the DB. API routes under `/api/semesters`, `/api/shiftings/[id]/{configure,activate}`, `/api/academic-years/archived`. Admin UI: `/semesters` matching the mockup — academic year cards with nested First/Second Shifting sub-cards, status pills, the verbatim activation warning banner, inline (non-drawer) Add Semester form, a Configure Shifting modal dialog, and a collapsible Archived Academic Years section (lazy-fetches only when expanded). |
| 7 | School management (admin CRUD + haversine helper) | Done (untested against live DB) | Full stack built: `src/lib/validation/school.ts` (Zod), `src/lib/services/school-service.ts` (CRUD, supervisor assign/reassign with logged history, check-in override, blocked-delete-with-interns), API routes under `/api/schools`, admin UI (`/schools` list with Secondary/Elementary tables, Add School drawer with map-picker placeholder + lat/long fallback + geofence radius slider, `/schools/[id]` detail with Overview/Interns/Resolved Alerts tabs). "View Profile"/"Unassign" buttons on the Interns tab are stubbed (no handler yet) — wire up once step 8 (accounts) exists. |
| 8 | Account management (admin CRUD, CT self-reg + approval, supervisor-scoped intern creation) | Done (untested against live DB) | Full stack built. `validation/account.ts` + `services/account-service.ts`: admin intern edit-for-corrections/delete (never create, per PRD §6.3), supervisor full CRUD, and supervisor-scoped intern creation (`createInternBySupervisor` — schoolId is NEVER taken from the request body, always `actor.supervisorSchoolId`, per CLAUDE.md). `services/ct-approval-service.ts`: pending/active CT lists, CT detail (with per-intern session/absence progress), approve, delete/reject. API routes under `/api/accounts/*` (admin) and `/api/supervisor/interns` (supervisor-scoped create/list, ready for the mobile Supervisor surface). Admin UI: `/accounts` with Interns (search, Edit drawer "for corrections only", 4-tab View detail — DTR/Sessions/Evaluations/Documents sub-tabs currently show "not yet available" since those modules don't exist)/Supervisors (add/edit/delete)/Cooperating Teachers (Pending Approval with Approve/Delete, Active with View/Delete, CT detail drawer) tabs. Wired the previously-stubbed "View Profile" button on Schools > Interns tab to the same intern detail sheet. **Left intentionally unwired:** "Unassign" on Schools > Interns — PRD §6.4 fixes an intern's school at creation and never defines a distinct unassign action; disabled with a tooltip rather than guessing at semantics. **Still open:** the CT self-registration password-delivery gap noted below — approval doesn't yet solve it (approving just flips status to ACTIVE; the CT still doesn't know their password). |
| 9 | Geofenced attendance | Not started | `src/lib/timezone.ts` (Manila day-boundary/cutoff helpers) done ahead of schedule since attendance depends on it. |
| 10 | Teaching sessions & evaluation | Not started | |
| 11 | Documents (Supabase Storage) | Not started | Supabase client not yet initialized — waiting on Supabase credentials. |
| 12 | At-risk detection & alerts | Not started | |
| 13 | Dashboards | Not started | |
| 14 | Reports + CSV export | Not started | |
| 15 | Notifications (in-app + Web Push) | Not started | VAPID keys generated and in `.env` (not committed) / documented in `.env.example`. |
| 16 | Admin ops screens (audit log viewer, config settings UI) | Not started | |
| 17 | Seeding, testing, docs | Not started | |

## Blocked

- **DATABASE_URL / DIRECT_URL / SUPABASE_SERVICE_ROLE_KEY** — user chose Supabase Postgres (2026-09-11) and sent the project URL (`https://msjfldpidpukuljkpdnf.supabase.co`) and publishable/anon key, both saved to `.env`. Still need: the Postgres connection string(s) from Supabase's Database Settings page (pooled for `DATABASE_URL`, direct for `DIRECT_URL` — Prisma Migrate needs the direct one) and the service role key for server-side Storage access. Until these arrive: no migration can run, so nothing that needs a live query can be verified end-to-end — every screen/API built so far is type-checked and build-verified but NOT verified against real data. Login screens were visually confirmed via a Playwright screenshot against the dev server (looks correct, matches mockups) but sign-in itself can't be tested without a database.
- Local disk was under 3GB free as of this session (started at 0 free, cache-cleaned to ~13GB, consumed by installs since). Check `df -h /c` before any large install; already close to full again.
- **Deprioritized fix:** `npm audit` still flags 3 high-severity issues, all in Prisma CLI's own transitive `@prisma/config` -> `deepmerge-ts`/`mysql2` chain (dev-time only, unreachable from the deployed app since we only use PostgreSQL). Chasing a clean audit would require downgrading Prisma below the version we need; left as-is.
- **CT self-registration password delivery gap** — see module 8 above. Registered CTs currently have no way to learn their initial password. Needs a product decision (see PROGRESS.md module 8 note) before this flow is actually usable end-to-end.

## Open PRD §10 questions still unresolved (do not silently pick an answer — see CLAUDE.md / skill guidance)

See `PRD.md` §10 in full. Flagged again here because they gate specific modules:
- Q1 Time Out rule (clock time vs minimum-hours) — gates attendance module (#9). Drafted default: configurable clock time, global + per-school override — implementing this drafted default behind config, but the real rule needs CTE confirmation.
- Q4 Excused-day counting toward thresholds — gates alerts module (#12). Drafted default: excluded from all three counts.
- Q5 INCOMPLETE day counting — gates attendance rate calc (#9/#13/#14). Drafted default: counted for pace, excluded from "present" rate.
- Q6 GPS failure manual override — gates attendance module (#9). Schema now includes `AttendancePunchSource` (`DEVICE`/`MANUAL_SUPERVISOR`) to support a supervisor-recorded manual punch, but the UI/API for it isn't built and the PRD doesn't confirm this is wanted — flagging for confirmation before building it.
- Q7 CT↔intern assignment mechanism — gates sessions module (#10) and account management (#8). UI_FLOW_SPEC.md confirms the deck never shows a screen that performs this assignment either. Schema puts `cooperatingTeacherId` on `InternProfile` (nullable), assignable by supervisor or admin — needs confirmation of who.
- Q8 Behind-on-pace formula — gates alerts (#12). `FlaggingRuleConfig.behindPaceTolerance` added to schema to hold whatever tolerance is confirmed.
- Q10 Document type/size constraints — gates documents module (#11).
- Q12 Geolocation spoofing — recommended mitigation (impossible-travel check) not yet designed.

## UI reference

`UI_FLOW_SPEC.md` (repo root) is the full transcription of `CTE-PPT-FLOW.pdf`'s 44 slides — read this instead of the PDF for screen layout/copy/navigation. Section 7 of that doc lists 10 PPT-vs-PRD discrepancies.
